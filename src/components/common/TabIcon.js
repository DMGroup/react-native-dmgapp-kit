/** * TabBar图标 */'use strict';import React, { Component } from 'react';import { StyleSheet, Text, View } from 'react-native'import Icon from 'react-native-vector-icons/FontAwesome';const styles = StyleSheet.create( {  tabIcon: {    alignSelf: 'center',  },  tabText: {    fontSize: 12,    marginTop: 2,  }} );const iconSize = 22;const selectColor = "#E2354E";const unSelectColor = '#999';export class TabIcon1 extends Component {  render() {    const color = this.props.selected ? selectColor : unSelectColor;    return (      <View style={ styles.tab }>        <Icon name="home" style={ styles.tabIcon } size={ iconSize } color={ color }/>        <Text style={[ styles.tabText, { color: color } ]}>          { this.props.title }        </Text>      </View>    );  }}export class TabIcon2 extends Component {  render() {    const color = this.props.selected ? selectColor : unSelectColor;    return (      <View>        <Icon name="edit" style={ styles.tabIcon } size={ iconSize } color={ color }/>        <Text style={[ styles.tabText, { color: color } ]}>          { this.props.title }        </Text>      </View>    );  }}